# Aggiungi queste configurazioni al tuo application.yml

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: ellestudiopilates@gmail.com
    password: ${MAIL_PASSWORD:your-app-password-here}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
